ocean:
  horiz_mesh_variables:
    - indexToCellID
    - indexToEdgeID
    - indexToVertexID
    - cellsOnCell
    - nEdgesOnCell
    - edgesOnCell
    - verticesOnCell
    - cellsOnEdge
    - edgesOnEdge
    - nEdgesOnEdge
    - verticesOnEdge
    - cellsOnVertex
    - edgesOnVertex
    - xCell
    - yCell
    - zCell
    - latCell
    - lonCell
    - xEdge
    - yEdge
    - zEdge
    - latEdge
    - lonEdge
    - xVertex
    - yVertex
    - zVertex
    - latVertex
    - lonVertex
    - areaCell
    - meshDensity
    - areaTriangle
    - dvEdge
    - dcEdge
    - angleEdge
    - kiteAreasOnVertex
    - weightsOnEdge
    - fCell
    - fEdge
    - fVertex
  vert_coord_variables:
    - minLevelCell
    - maxLevelCell
    - bottomDepth
    - vertCoordMovementWeights
  state_variables:
    - temperature
    - salinity
    - normalVelocity
  forcing_variables:
    - windStressZonal
    - windStressMeridional

mpas-ocean:
  vert_coord_variables:
    - restingThickness
  state_variables:
    - layerThickness

Omega:
  vert_coord_variables:
    # [MinLayerCell, MaxLayerCell, BottomGeomDepth,
    # VertCoordMovementWeights, RefPseudoThickness]
    - RefPseudoThickness
  state_variables:
    - PseudoThickness
    # SurfacePressure is an Omega-only field (relative/gauge pressure) with no
    # MPAS-Ocean equivalent, so it is not in the mpaso_to_omega.yaml var map
    - SurfacePressure
