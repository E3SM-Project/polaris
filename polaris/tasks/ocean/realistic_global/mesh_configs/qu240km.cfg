# Options related to the vertical grid
[vertical_grid]

# the type of vertical grid
grid_type = tanh_dz

# Number of vertical levels
vert_levels = 16

# Depth of the bottom of the ocean
bottom_depth = 3000.0

# The minimum layer thickness
min_layer_thickness = 3.0

# The maximum layer thickness
max_layer_thickness = 500.0


# Options for the forward run on this mesh.  This is qualitatively the same
# mesh as u.oi240.lr240, so these must stay in sync with it; a test checks
# that they do.
[realistic_global_forward]

# a Leith closure plus tracer mixing, scaled relative to a reference cell width.
# The reference is this mesh's own width, so the coefficients above are used as
# given; leaving it at the 30 km default would scale them by 240/30.
tracer_del2 = 10.0
tracer_del4 = 1.2e11
use_Leith_del2 = True
hmix_scaling = ref_cell_width
hmix_ref_cell_width = 240.0e3

use_frazil_ice_formation = True
